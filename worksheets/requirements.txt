# ReportLab 4.4.3 calls hashlib.md5(usedforsecurity=False), which needs
# Python 3.9, but still advertises Requires-Python >=3.7 — so on 3.8 pip
# installs it happily and every PDF then dies with a TypeError. 4.4.2 is
# the last release that actually runs there.
reportlab>=4.0,<4.4.3 ; python_version < "3.9"
reportlab>=4.0 ; python_version >= "3.9"
